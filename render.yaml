services:
  - type: web
    name: volk-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: volk-db
          property: connectionString

databases:
  - name: volk-db
    plan: free
